#!/bin/sh
set -e

# first arg is `-f` or `--some-option`
if [ "${1#-}" != "$1" ]; then
	set -- php "$@"
fi

useradd -m  -u 1000 -g 1000 -d  /home/developer developer
usermod -G www-data developer
exec "$@"
